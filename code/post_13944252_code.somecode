<code>corpus &lt;- Corpus(VectorSource(vec), readerControl=list(language="en")) 
corpus &lt;- tm_map(corpus, tolower)
corpus &lt;- tm_map(corpus, removePunctuation)
corpus &lt;- tm_map(corpus, removeWords, stopwords("english"))
corpus &lt;- tm_map(corpus, stripWhitespace)
corpus &lt;- tm_map(corpus, removeNumbers)
...snip removing several custom lists of stopwords...
corpus &lt;- tm_map(corpus, stemDocument)
dtm &lt;- DocumentTermMatrix(corpus, control=list(minDocFreq=2, minWordLength=2))
</code>
<code>LDA(dtm, 30)
</code>
<code>  "Each row of the input matrix needs to contain at least one non-zero entry". 
</code>