<code>@files=glob("*_classification.dat");
foreach my $file (@files)
{
    open(IN,$file) or die $!;
    &lt;IN&gt;;
    while(&lt;IN&gt;)
    {
       $_='~s/blue/red/g';
       print IN $file;
    }

   close(IN)
}
</code>